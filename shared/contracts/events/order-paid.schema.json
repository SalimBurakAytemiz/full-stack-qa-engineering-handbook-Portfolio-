{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.internal/contracts/events/order-paid.schema.json",
  "title": "order.paid domain event payload",
  "description": "The real, persisted shape of the `payload` column (JSON-encoded) on the `events` table for event_type='order.paid', produced by QA-DEMO-SYSTEM/backend/src/services/events.service.js#createOrderPaidEvent. This is the canonical event contract Section 27 asks for — WebSocket push and any future consumer must agree with this shape, not invent their own.",
  "type": "object",
  "required": ["orderId", "userId", "total"],
  "properties": {
    "orderId": { "type": "integer" },
    "userId": { "type": "integer" },
    "total": { "type": "number" }
  },
  "additionalProperties": false
}
