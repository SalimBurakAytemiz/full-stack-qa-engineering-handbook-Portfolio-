# Post-Codex Consolidated Fix Campaign — Finding Closure Matrix

Every finding from the consolidated fix campaign's P0-P3 list, with its
real disposition, the files actually changed, how it was validated, and
the test/evidence proving the fix. No finding was marked FIXED without
a corresponding validation step actually run this session — see
`.ai/TEST-STATUS.md` for the exact regression results referenced below.

There was no separate, independently-authored external Codex audit
report in this session — the campaign prompt itself supplied the 13
findings' descriptions as the authoritative defect list, per its own
explicit instruction ("treat Codex findings as authoritative unless
repository evidence proves a false positive"). Every finding below was
independently re-verified against real repository content before being
fixed (see the "Verification" column) — none were fixed blindly on the
prompt's word alone, and none were dismissed without documented
evidence.

| Finding | Status | Files Changed | Verification | Test/Evidence |
|---|---|---|---|---|
| **P1-01** Digital Twin personal truth | FIXED | `01-SALIM-BURAK-DIGITAL-TWIN/registry/domain-state.yaml`, `source-provenance.yaml`, `tool-state.yaml` | Confirmed 2 internal derivation-inconsistency bugs (streaming exposure understated vs. its own `owner_fact`; precious-metals exposure ignored a stronger existing category) and 1 genuine gap (BDD/Cucumber/Device-Farms/ADB/Trello claimed as EXPERIENCE only in the superseded `QA-COMPETENCY-MAP.md`, absent from current canonical source) — all confirmed by direct file comparison, not assumed | `npm run registry:validate` PASS (0 errors); 2 conflict_log entries added documenting the corrections; 4 new tool-state.yaml entries added as `USER_CONFIRMATION_REQUIRED` matching the existing Jira/Confluence precedent, not silently re-promoted to EXPERIENCE |
| **P1-02** BUG-AUTH-EDU-001 evidence truth | FIXED | `06-EVIDENCE/evidence.yaml`, `02-FULL-STACK-QA-HANDBOOK/04-MANUAL-TESTING/README.md` | Confirmed real: `evidence.yaml`'s `label`/`produced_by` said "reproduced defect...regression test added," directly contradicting the historical `EXECUTION.md`'s own "NOT REPRODUCED" verdict and "no new test added" reasoning (line-by-line comparison). All other narrative references (README.md, PHASE-4-CLOSEOUT.md, ARCHITECTURE.md, ROADMAP.md) already said NOT REPRODUCED correctly — only 2 files needed correction | Historical `EXECUTION.md` NOT modified (confirmed via `git diff`); corrected entries now match it exactly |
| **P1-03** API CI coverage claims | FIXED | `.github/workflows/ci.yml`, `06-EVIDENCE/evidence.yaml`, `shared/registry/catalog/labs.yaml`, `shared/registry/relationships/relationships.yaml` | Confirmed real: only `api:test:postman` (public/schema) ran in CI; `api:test:auth`/`orders-payment`/`notifications`/`db` existed and passed locally since P5.3-P5.7 but were never CI-wired | 4 new CI jobs added (`api-auth-tests`, `api-orders-payment-tests`, `api-notifications-tests`, `api-db-validation`), each locally verified this session against a fresh seed: auth 19/19 req 42/42 assert PASS, orders-payment 64/64 req 118/118 assert PASS (after 1 confirmed flake re-run — see TEST-STATUS.md), notifications 20/20 req 48/48 assert PASS, db-validation 17/17 scenarios 97/97 assert PASS. `evidence.yaml`'s single lumped entry split into 5 per-package entries |
| **P1-04** Relationship graph semantics | FIXED | `shared/registry/relationships/relationships.yaml`, `scripts/registry/validate-registry.mjs` | Confirmed both named examples real: `competency.cicd.jenkins PRACTICED_IN lab.cicd.github-actions` (Jenkins never ran in that lab) and `competency.observability.elastic-log-analysis PRACTICED_IN lab...` + `USES_TOOL tool.elastic` (competency's own `repository.status: NOT_PRACTICED`, no real Elastic runtime anywhere in the repo) | Both edges corrected (Jenkins edge removed — already covered by the existing deliberate `RELATED_TO` edge per D7; Elastic edge changed to `RELATED_TO` with an explanatory note). A NEW structural validator check added: no `PRACTICED_IN` edge may originate from a competency with `repository.status: NOT_PRACTICED` — makes this class of bug fail automatically going forward, not just fixed once. Also found and fixed a `pattern.retry VALIDATES_FLOW lab.cicd.github-actions` edge with the same defect (see P2-05). Regression test: `validate-registry.regression.test.mjs` test 9, PASS |
| **P1-05** Claim-level provenance enforcement | FIXED | `01-SALIM-BURAK-DIGITAL-TWIN/registry/claims.yaml` (new), `shared/registry/schemas/claim.schema.json` (new), `scripts/registry/validate-registry.mjs` | The described gap was real and reproduced: before this fix, inserting a fabricated `what_i_did` bullet into `professional-experience.yaml` did NOT fail validation (only a file-level "some source exists" check ran) | 56 claim entries created (1:1 with every real `what_i_did`/`other_professional_project_exposure` bullet), each with its own `source_id`. Validator now checks both directions (every bullet has a matching SOURCED claim; every claim matches a live bullet). **Negative test proven**: a fabricated bullet was injected into the real file and confirmed to produce a non-zero exit with message "unsourced professional claim," then reverted (`git diff` empty after revert). **Positive test proven**: full 56/56 claims verified 1:1. Both codified as permanent regression tests 1 and (implicitly covered by every other PASS) in `validate-registry.regression.test.mjs` |
| **P2-01** Domain catalog ownership | FIXED | `shared/registry/catalog/domains.yaml`, `shared/registry/schemas/domain-catalog.schema.json` (new), `scripts/registry/validate-registry.mjs`, `scripts/registry/lib/build-index-markdown.mjs`, `03-DOMAINS/README.md` | Confirmed real: `domains.yaml` carried an `exposure` field using personal-exposure vocabulary, contradicting its own header comment ("independent of any one person's exposure"); personal `domain-state.yaml` had 10 domains vs. the catalog's 5 | New `domain-catalog.schema.json` (maturity D0-D5 only, no exposure field) replaces the reused personal schema; 5 missing universal catalog entries added (`domain.commerce.oms`, `.financial.crypto`, `.financial.trading-precious-metals`, `.healthcare`, `.telecom`); new structural foreign-key check added (every personal `domain_id` must resolve to a real catalog entry). **Bug found and fixed during this work**: the index generator still referenced the old `exposure` field post-rename, silently printing "undefined" for every domain — the drift check didn't catch it because both sides used the same broken logic; fixed and verified the generated content is now correct. Regression test 2, PASS |
| **P2-02** Windows LF/CRLF portability | FIXED | `scripts/registry/validate-registry.mjs`, `QA-DEMO-SYSTEM/backend/tests/graphql-contract-drift.test.js`, `.gitattributes` (new) | Confirmed both checks were byte-for-byte string comparisons with no EOL normalization | Both comparisons now normalize CRLF→LF before comparing. `.gitattributes` added (`* text=auto eol=lf`) as defense-in-depth. **All 4 required scenarios proven**: CRLF-only diff in REGISTRY-INDEX.md → PASS; real content drift in REGISTRY-INDEX.md → FAIL; CRLF-only diff in schema.graphql → PASS; real schema drift in schema.graphql → FAIL. Regression tests 4-8, all PASS |
| **P2-03** Synchronize documentation | FIXED | `03-DOMAINS/README.md`, `02-FULL-STACK-QA-HANDBOOK/08-SOFTWARE-ARCHITECTURE-FOR-QA/README.md` | Confirmed real, found while fixing P2-01: `03-DOMAINS/README.md`'s summary table said Streaming/Mobile D1 DOCUMENTED and Insurance D0 INDEXED, but each domain's own page header (and `.ai/ARCHITECTURE-STATE.md`) already recorded D2 QA_MAPPED from an earlier deep-dive round the table was never updated for | Table corrected to D2 QA_MAPPED for all 3, with a pointer to `domains.yaml` as canonical source; "Why Insurance is D0" section heading/text corrected to match | Repo-wide relative-link check: 803/803 resolve, 0 broken (improved from a prior 802/803 + 1 known false positive) |
| **P2-04** JMeter evidence split | FIXED | `06-EVIDENCE/evidence.yaml`, `shared/registry/relationships/relationships.yaml` | The underlying `EXECUTION.md`/`DECISIONS.md` records were already honest and detailed; the bug was specifically `evidence.yaml`'s single lumped entry (label "JMeter load test with JTL-content fail-gate," maturity E3_EXECUTED) which did not distinguish config vs. fail-gate-logic vs. real-execution | Split into 3 entries: `.jmeter-workload-design` (E2_IMPLEMENTED, the real .jmx plan), `.jmeter-fail-gate-logic` (E3_EXECUTED, 7/7 real fixture-based unit tests — re-run this session, PASS), `.jmeter-real-execution-blocked` (E1_DOCUMENTED, explicitly NOT a successful load run). Real JMeter binary re-attempted this session: identical `ForbiddenClassException`, reproducible. Fail-gate wrapper re-run against the real (failing) binary this session: correctly reports `LAUNCH_OR_RUNTIME_ERROR`, exit 1 |
| **P2-05** Retry status | FIXED | `shared/registry/catalog/patterns.yaml`, `shared/registry/relationships/relationships.yaml` | Confirmed real via `grep -r retry` across `backend/src` and `automation-labs`: zero matches; RETRY.md's own text already said the fail-gate does NOT auto-retry | `pattern.retry` status corrected `IMPLEMENTED` → `DOCUMENTED_ONLY`. A stale note claiming a "CI/test-execution level retry policy" was also found false (same grep covers `automation-labs/`) and removed. A `pattern.retry VALIDATES_FLOW lab.cicd.github-actions` relationship edge (same false-execution defect as P1-04) was removed. Chose option (B) — corrected the status — not fake retry logic added merely for a badge |
| **P2-06** Exactly-once terminology | FIXED | `02-FULL-STACK-QA-HANDBOOK/08-SOFTWARE-ARCHITECTURE-FOR-QA/README.md` | Confirmed real: the text called `UNIQUE(order_id, event_type)` an "exactly-once delivery" mechanism, conflating storage-layer deduplication with transport-layer delivery semantics | Rewritten to distinguish persisted-event uniqueness/deduplication (what the constraint provides), processing idempotency (this repo's real, tested property), at-least-once delivery (the realistic queue guarantee the deduplication pattern actually generalizes to), and delivery exactly-once-ness (a transport/consumer-protocol property, not tested or claimed here) |
| **P3-01** TR comment standard | FIXED | `scripts/registry/validate-registry.mjs`, `scripts/registry/lib/build-index-markdown.mjs`, `scripts/registry/build-indexes.mjs` | Reviewed all 3 generator/validator source files for meaningful, non-obvious-logic comments lacking a TR: companion, per the established repo convention | Added `// TR:` companions to the 3 files' header blocks (data ownership, generation-sharing rationale, do-not-hand-edit rationale) and to newly-added logic blocks (claim provenance, domain FK, relationship semantics, EOL normalization) — not added to routine section-divider comments, per "do not add useless comments to every line" |
| **P3-02** Whitespace cleanup | FIXED | (verification only — no residual defect found) | `git diff --check` run against the full staged diff at multiple points during this campaign | Clean at every check — 0 whitespace errors reported |

## Summary

- **P0: 0** (none were ever claimed)
- **P1: 5 / 5 FIXED**
- **P2: 6 / 6 FIXED**
- **P3: 2 / 2 FIXED**
- **FALSE_POSITIVE: 0** — every finding investigated was confirmed real against concrete repository evidence before being fixed; none were dismissed
- **EXTERNALLY_BLOCKED: 0** — every finding was fixable within this session's environment

No finding was marked FIXED without the validation step listed in its
row actually being run this session — see `.ai/TEST-STATUS.md` for the
full regression suite results this matrix's "Test/Evidence" columns
reference.
