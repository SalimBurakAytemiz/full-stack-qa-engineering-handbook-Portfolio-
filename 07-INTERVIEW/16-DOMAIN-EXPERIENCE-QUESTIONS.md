# Domain Experience Questions

Answers sourced from `01-SALIM-BURAK-DIGITAL-TWIN/registry/domain-state.yaml`
and `professional-experience.yaml`.

## "Walk me through your e-commerce QA experience."

`domain.commerce.ecommerce` is `PROFESSIONAL_EXECUTED`, owned by
`professional-experience.yaml#pro.ecommerce.skechers-turkey` — a named
professional context (Skechers Turkey E-commerce), not sanitized,
because the source data named it directly. This repository's
`QA-DEMO-SYSTEM` order/payment/notification flow is a synthetic
re-implementation used for repository practice, explicitly not a
recreation of the professional system — see
`03-DOMAINS/02-COMMERCE-RETAIL/E-COMMERCE/README.md`.

## "Do you have FinTech experience?"

Yes, at `PROFESSIONAL_EXECUTED` exposure, but the professional context is
sanitized (`professional-experience.yaml#pro.fintech.stablex-like`) — a
"-like" label because the source data itself gave a sanitized name, not
because it was invented. The repository has no dedicated FinTech
backend; `03-DOMAINS/01-FINANCIAL-SERVICES/FINTECH/README.md` maps onto
the Commerce order/payment flow as the closest real analog and says so
explicitly, rather than pretending a separate FinTech codebase exists.

## "What about mobile / multi-country work?"

`domain.mobile.multi-country` is `PROFESSIONAL_EXECUTED`
(`professional-experience.yaml#pro.mobile.defacto-like`, sanitized).
No corresponding executable lab exists in this repository — that gap is
stated plainly in `03-DOMAINS/04-MOBILE-DIGITAL-PLATFORMS/README.md`
rather than papered over.

## "Insurance domain?"

No professional or repository claim. `domain.insurance` is `NONE`
exposure. `03-DOMAINS/01-FINANCIAL-SERVICES/INSURANCE/README.md` exists
only as a learning/transferable-methodology placeholder — explicitly not
a professional claim, per Section 19 of the transformation spec.
