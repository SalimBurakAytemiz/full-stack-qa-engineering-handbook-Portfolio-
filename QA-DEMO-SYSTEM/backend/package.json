{
  "name": "qa-demo-system-backend",
  "version": "0.1.0",
  "private": true,
  "description": "QA Demo System backend (Phase 4, P4.1 — Authentication + Products skeleton)",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:seed": "node src/database/seed.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
